---
layout: base.njk
title: Anna's song vault.
---

<div class="max-w-4xl mx-auto">
  <h2 class="text-4xl font-bold text-gray-800 mb-6">Welcome. Pick an era.</h2>

  <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
    {% for era in eras %}
      <a
        href="/era/{{ era.slug }}/"
        aria-label="{{ era.name }}"
        class="group block rounded-xl overflow-hidden bg-white border border-gray-200 shadow-md hover:shadow-lg hover:-translate-y-0.5 transition"
      >
        <div class="relative pb-[100%]">
          <div class="absolute inset-0 flex flex-col">
            <div class="flex-1 bg-gray-100 overflow-hidden">
              <img
                src="/eras/{{ era.slug }}/{{ era.slug }}.png"
                alt="{{ era.name }}"
                class="w-full h-full object-cover"
                loading="lazy"
              >
            </div>
            <div class="p-4 border-t border-gray-200">
              <p class="text-gray-700">{{ era.description }}</p>
            </div>
          </div>
        </div>
      </a>
    {% endfor %}
  </div>
</div>
