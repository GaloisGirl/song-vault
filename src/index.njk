---
layout: base.njk
title: Anna's song vault.
---

<div class="max-w-4xl mx-auto">
  <h2 class="text-4xl font-bold text-gray-800 mb-6">Welcome. Pick an era.</h2>

  <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
    {% for era in eras %}
      <a
        href="/era/{{ era.slug }}/"
        class="h-48 sm:h-56 bg-white rounded-xl shadow-md border border-gray-200 flex items-center justify-center p-6 text-center text-xl font-semibold text-gray-800 hover:shadow-lg hover:-translate-y-0.5 transition"
      >
        {{ era.name }}
      </a>
    {% endfor %}
  </div>
</div>
